================================================================================
TASK-001: Add ProjectUsersPath and SingleProjectUserPath helpers to internal/utils
Date: 2026-07-03T15:50:00Z
================================================================================

Summary:
Added two new path helper functions to internal/utils/path_helpers.go:
ProjectUsersPath(orgID, projectID) and SingleProjectUserPath(orgID, projectID,
userID). Both use the existing resourcePath helper following the same pattern
as all other helpers in that file. go build ./... and go test ./internal/utils/...
both pass.

Files Changed:
- internal/utils/path_helpers.go (modified — added ProjectUsersPath and SingleProjectUserPath)
- .plans/tasks-v2beta1-project-users.yml (modified — task-001 marked completed)

Key Decisions:
- Doc comment style left consistent with the rest of the file ("// Returns endpoint
  path for...") rather than switching to idiomatic Go "// FuncName returns..." to
  avoid a mixed style in a single file; a follow-up can normalise all comments.
- No path helper tests added in this task — acceptance criteria did not require them
  and task-003 covers the testing layer. The gap (no coverage for any path helpers)
  is noted for a future housekeeping task.
- utils.UserID validator does not yet exist despite the task description saying it
  does; task-002 will need to add it before the service can validate userID params.

Next Task: task-002

================================================================================
TASK-002: Implement ProjectUserService (types, service, interface, client wiring)
Date: 2026-07-03T16:30:00Z
================================================================================

Summary:
Added utils.UserID validator to internal/utils/validate_id.go (UUID format).
Created v2beta1/project_users.go with ProjectUser, ListProjectUsersResponse,
GetProjectUserResponse, AddProjectUserRequest, UpdateProjectUserRequest types and
the projectUserService struct implementing List, Add, UpdateRole, and Remove.
Add returns error only (no response body; HTTP 201 with no content). Remove
returns error only (HTTP 204 with no content). UpdateRole and Remove both validate
userID via utils.UserID. Added ProjectUserService interface and compile-time check
to interfaces.go. Wired client.ProjectUsers in client.go and incremented services
count from 4 to 5.

Files Changed:
- internal/utils/validate_id.go (modified — added UserID constructor)
- v2beta1/project_users.go (created — types, projectUserService, all 4 methods)
- v2beta1/interfaces.go (modified — added ProjectUserService interface and compile-time check)
- v2beta1/client.go (modified — added ProjectUsers field and wiring; services count 4→5)

Key Decisions:
- services count in task description said "increment from 6 to 7" but actual code had 4;
  bumped to 5 (4 existing + 1 new) to reflect reality.
- utils.UserID uses UUID format (long form xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx),
  consistent with OrganizationID and ProjectID.
- Add and Remove discard the api.Response (no body expected) and return only error,
  consistent with the task spec (HTTP 201/204 no content).

Next Task: task-003

================================================================================
TASK-003: Write tests for ProjectUserService
Date: 2026-07-03T17:30:00Z
================================================================================

Summary:
Fixed type mismatches in v2beta1/project_users.go: ProjectUser.Role string →
ProjectRoles []string, AddProjectUserRequest Email/Role → UserID/ProjectRoles,
UpdateProjectUserRequest Role → ProjectRoles, all matching the PRD spec.
Also fixed client.go services count from 5 to 6 (six services are now wired).
Created v2beta1/project_users_test.go with the full AGENTS.md test matrix for
all four methods (List, Add, UpdateRole, Remove). Tests use table-driven
_InvalidOrgID, _InvalidProjectID, _InvalidUserID sub-tests; Add and UpdateRole
tests also unmarshal mock.lastBody to assert request serialisation. All tests
pass with go test -race ./v2beta1/... and golangci-lint reports 0 issues.

Files Changed:
- v2beta1/project_users.go (modified — corrected ProjectUser, AddProjectUserRequest,
  UpdateProjectUserRequest types to use ProjectRoles []string per PRD spec)
- v2beta1/client.go (modified — services count 5→6)
- v2beta1/project_users_test.go (created — full test coverage for all 4 methods)

Key Decisions:
- Types corrected per PRD spec: ProjectRoles []string instead of Role string,
  AddProjectUserRequest uses UserID+ProjectRoles instead of Email+Role.
- services count corrected to 6 (Organizations, Projects, Instances, Databases,
  DatabaseBackups, ProjectUsers).
- Used table-driven tests for all _InvalidID variants (matching AGENTS.md guidance).
- Add_QuickCancellation included even though not listed in the task acceptance
  criteria explicitly, to match List/Add symmetry in the test matrix.

Next Task: task-004
